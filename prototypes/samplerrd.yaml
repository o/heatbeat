--- # Sample RRD

rrd:
	filename:
	step:

datastores:
   - name:   
     type:   
     heartbeat:
     min:  
     max:
          
rras:
   - cf:
     xff:
     steps:
     rows:

graph:
    options:
        title:
        vertical-label:  
        base:
        lower-limit:
        upper-limit:    

    gprints:
        - vname:
          format:

    elements:
        - value:
          type:
          color:
          legend:
              
definitions:
    defs:
        - vname:
          dsname:
          cf:
          step:
          start:
          end:
          reduce:
          
    cdefs:
        - vname:
          rpn:
          
    vdefs:
        - vname:
          rpn:                  
              
collection:
      type:
      string:
      inputs:
      outputs: